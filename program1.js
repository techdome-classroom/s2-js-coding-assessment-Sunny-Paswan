/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    var count = 0;
    for(let i=0; i<s.length; i++){
        if(s[i] == '(' ||)
    }
};

module.exports = { isValid };


