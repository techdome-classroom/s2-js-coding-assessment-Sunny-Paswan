/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    container = [];

    for char in s:
        if char =='(':
        MediaStreamTrack.append('(')
};

module.exports = { isValid };


