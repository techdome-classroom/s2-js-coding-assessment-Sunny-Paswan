/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let container = [];

    for(let char in s){
        if char =='(':
        container.append('(')
        else if char == '{':
        container.append('{')
        else if char == '[':
        container.append('[')
        else if char == ')':
        if not container or 

};

module.exports = { isValid };


